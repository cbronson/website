version: 0.2
phases:
  prebuild:
    commands:
      - echo "installing dependencies..."
      - npm install
  build:
    commands:
      - echo "building..."
      - npm run build
artifacts:
  files:
    - build/