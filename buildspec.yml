version: 0.2
phases:
  pre_build:
    commands:
      - echo "installing dependencies..."
      - npm install
  build:
    commands:
      - echo "building..."
      - npm run build
artifacts:
  files:
    - build/